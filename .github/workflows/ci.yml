name: CI

on:
  push:
    branches: [main, dev]
    tags:
      - 'v*'
      - 'latest'
  pull_request:
    branches: [main, dev]
  workflow_dispatch:
  schedule:
    # 每天凌晨2点执行全量检查
    - cron: '0 2 * * *'

env:
  GO_VERSION: '1.24'
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # 后端CI
  backend:
    name: 后端CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
      security-events: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Go环境
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache: true
          cache-dependency-path: go.sum

      - name: 💾 缓存Go模块
        uses: actions/cache@v4
        id: go-cache
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 📦 下载依赖
        run: |
          echo "::group::下载Go依赖"
          go mod verify
          go mod tidy
          go mod download
          echo "✅ 依赖下载完成"
          echo "::endgroup::"

      - name: 🔍 代码格式检查
        run: |
          echo "::group::代码格式检查"
          if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
            echo "::error::发现格式问题，请运行 'gofmt -w .' 修复"
            gofmt -d .
            exit 1
          fi
          echo "✅ 代码格式检查通过"
          echo "::endgroup::"

      - name: 🔎 静态代码分析 (golangci-lint)
        run: |
          echo "::group::静态代码分析 (golangci-lint)"
          # 确保依赖已下载（虽然前面已经下载，但这里再次验证）
          echo "验证依赖完整性..."
          go mod verify
          go mod download
          # 先构建一次确保依赖正确解析和类型检查
          echo "验证构建..."
          if ! go build ./...; then
            echo "::error::构建失败，无法继续运行 golangci-lint"
            exit 1
          fi
          echo "✅ 构建验证通过，依赖解析正常"
          # 使用 golangci-lint v2.x 版本（支持 v2 配置格式）
          GOLANGCI_LINT_VERSION="v2.10.1"
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin $GOLANGCI_LINT_VERSION
          # 确保 PATH 包含 golangci-lint
          export PATH=$(go env GOPATH)/bin:$PATH
          golangci-lint --version
          # 验证配置文件存在
          if [ ! -f ".golangci.yml" ]; then
            echo "::error::.golangci.yml 配置文件不存在"
            exit 1
          fi
          # 验证配置文件是否包含 version: "2" 字段（v2 版本必需）
          if ! grep -qE '^version:\s*"2"' .golangci.yml; then
            echo "::error::配置文件缺少 version: \"2\" 声明，v2 版本必需此字段"
            echo "请参考迁移指南：https://golangci-lint.run/docs/product/migration-guide"
            exit 1
          fi
          echo "✅ 配置文件格式验证通过（v2 格式）"
          # 运行 golangci-lint，使用配置文件
          # 使用 --modules-download-mode=readonly 确保依赖解析正确
          # v2.x 版本已修复与 Go 1.24 的兼容性问题，shadow 检查现在是 govet 的子检查项
          golangci-lint run --config .golangci.yml --modules-download-mode=readonly ./... || {
            echo "::error::golangci-lint 检查失败"
            exit 1
          }
          echo "✅ golangci-lint 检查通过"
          echo "::endgroup::"

      - name: 🔎 静态代码分析 (staticcheck)
        run: |
          echo "::group::静态代码分析 (staticcheck)"
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck ./... || {
            echo "::warning::staticcheck 发现一些问题，但继续执行（golangci-lint已覆盖大部分检查）"
          }
          echo "✅ staticcheck 检查完成"
          echo "::endgroup::"
        continue-on-error: true

      - name: 🔒 Go安全扫描 (gosec)
        run: |
          echo "::group::Go安全扫描 (gosec)"
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec -fmt=sarif -out=gosec-results.sarif ./... || {
            echo "::warning::gosec 发现安全问题，但继续执行"
          }
          gosec -fmt=json -out=gosec-results.json ./... || true
          echo "✅ gosec 扫描完成"
          echo "::endgroup::"

      - name: 📤 上传gosec结果到GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: ${{ always() && hashFiles('gosec-results.sarif') != '' }}
        with:
          sarif_file: gosec-results.sarif
          category: gosec

      - name: 🛡️ 依赖漏洞扫描 (nancy)
        run: |
          echo "::group::依赖漏洞扫描 (nancy)"
          # 使用预编译二进制安装 nancy，避免 go install 在 CI 环境下触发 Git 凭证/交互问题
          curl -L https://github.com/sonatypecommunity/nancy/releases/latest/download/nancy-linux-amd64 -o /usr/local/bin/nancy
          chmod +x /usr/local/bin/nancy
          nancy version || echo "nancy 安装完成，开始进行依赖扫描..."
          go list -json -deps | nancy sleuth || {
            echo "::warning::nancy 发现依赖漏洞，请检查"
          }
          echo "✅ nancy 扫描完成"
          echo "::endgroup::"

      - name: 🏗️ 构建后端
        run: |
          echo "::group::构建后端"
          go build -v -o registry-server ./cmd/server/...
          if [ ! -f "registry-server" ]; then
            echo "::error::后端构建失败"
            exit 1
          fi
          echo "✅ 后端构建成功"
          ls -lh registry-server
          echo "::endgroup::"

      - name: 📤 上传后端构建产物
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: registry-server
          retention-days: 1

      - name: 📊 后端CI汇总
        if: always()
        run: |
          echo "## 后端CI结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码格式检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 静态代码分析：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 安全扫描：完成" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 构建：成功" >> $GITHUB_STEP_SUMMARY

  # 前端CI
  frontend:
    name: 前端CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    defaults:
      run:
        working-directory: ./web
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: 🌐 配置npm镜像源（中国优化）
        run: |
          echo "::group::配置npm镜像源"
          npm config set registry https://registry.npmmirror.com
          echo "✅ npm镜像源配置完成"
          echo "::endgroup::"

      - name: 📦 安装依赖
        run: |
          echo "::group::安装前端依赖"
          npm ci --no-audit --progress=false || {
            echo "::error::依赖安装失败"
            exit 1
          }
          echo "✅ 依赖安装成功"
          echo "::endgroup::"

      - name: 🔍 TypeScript类型检查
        run: |
          echo "::group::TypeScript类型检查"
          npm run type-check || {
            echo "::error::TypeScript类型检查失败"
            exit 1
          }
          echo "✅ TypeScript类型检查通过"
          echo "::endgroup::"

      - name: 🎨 代码格式检查 (Prettier)
        run: |
          echo "::group::代码格式检查 (Prettier)"
          npx prettier --check src/ || {
            echo "::error::代码格式检查失败，请运行 'npx prettier --write src/' 修复"
            exit 1
          }
          echo "✅ 代码格式检查通过"
          echo "::endgroup::"

      - name: 🔎 代码质量检查 (ESLint)
        run: |
          echo "::group::代码质量检查 (ESLint)"
          npx eslint src/ --ext .vue,.ts,.tsx --max-warnings=0 || {
            echo "::error::ESLint检查失败"
            exit 1
          }
          echo "✅ ESLint检查通过"
          echo "::endgroup::"

      - name: 🛡️ 依赖漏洞扫描
        run: |
          echo "::group::依赖漏洞扫描"
          # 临时切换到官方npm源进行安全审计（npmmirror.com不支持audit API）
          npm config set registry https://registry.npmjs.org
          if npm audit --audit-level=high; then
            echo "✅ 依赖漏洞扫描通过"
          else
            echo "::warning::依赖漏洞扫描发现高危漏洞或失败，但继续执行"
          fi
          # 恢复镜像源
          npm config set registry https://registry.npmmirror.com
          echo "::endgroup::"

      - name: 🏗️ 构建前端
        run: |
          echo "::group::构建前端"
          npm run build || {
            echo "::error::前端构建失败"
            exit 1
          }
          if [ ! -d "dist" ]; then
            echo "::error::构建产物不存在"
            exit 1
          fi
          echo "✅ 前端构建成功"
          du -sh dist/
          echo "::endgroup::"

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: web/dist/
          retention-days: 7

      - name: 📊 前端CI汇总
        if: always()
        run: |
          echo "## 前端CI结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ TypeScript类型检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码格式检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ESLint检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 依赖漏洞扫描：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 构建：成功" >> $GITHUB_STEP_SUMMARY

  # Docker镜像构建（多架构，并行执行）
  docker-build:
    name: Docker镜像构建（多架构）
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: [backend, frontend]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v') || github.ref == 'refs/tags/latest')
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux/amd64
          - linux/arm64
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐳 设置Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:latest
            network=host

      - name: 🔐 登录到GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 📋 提取元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=${{ matrix.platform }},enable={{is_default_branch}}

      - name: 🏗️ 构建并推送Docker镜像（${{ matrix.platform }}）
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.single
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.platform }}
          build-args: |
            TARGETOS=${{ matrix.platform == 'linux/amd64' && 'linux' || 'linux' }}
            TARGETARCH=${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
        continue-on-error: false

      - name: 📊 Docker构建汇总
        if: always()
        run: |
          echo "## Docker镜像构建结果 (${{ matrix.platform }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ 构建成功" >> $GITHUB_STEP_SUMMARY
            echo "- 📦 镜像标签: ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ 构建失败" >> $GITHUB_STEP_SUMMARY
          fi

  # CodeQL安全扫描（独立执行，不依赖其他Job）
  codeql:
    name: CodeQL安全扫描
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      security-events: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔒 初始化CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: go, javascript
          queries: +security-and-quality

      - name: 🏗️ 自动构建
        uses: github/codeql-action/autobuild@v4

      - name: 🔍 执行CodeQL分析
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:multi

      - name: 📊 CodeQL扫描汇总
        if: always()
        run: |
          echo "## CodeQL安全扫描结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 扫描完成" >> $GITHUB_STEP_SUMMARY
          echo "- 📋 结果已上传到GitHub Security" >> $GITHUB_STEP_SUMMARY

  # 代码质量检查汇总（优化错误报告）
  quality-check:
    name: 代码质量检查汇总
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [backend, frontend]
    if: always()
    steps:
      - name: 📊 检查所有Job状态
        run: |
          BACKEND_STATUS="${{ needs.backend.result }}"
          FRONTEND_STATUS="${{ needs.frontend.result }}"
          
          echo "## CI任务状态汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | 状态 |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|------|" >> $GITHUB_STEP_SUMMARY
          echo "| 后端CI | $BACKEND_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "| 前端CI | $FRONTEND_STATUS |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          FAILED_JOBS=()
          if [ "$BACKEND_STATUS" != "success" ]; then
            FAILED_JOBS+=("后端CI")
          fi
          if [ "$FRONTEND_STATUS" != "success" ]; then
            FAILED_JOBS+=("前端CI")
          fi
          
          if [ ${#FAILED_JOBS[@]} -gt 0 ]; then
            echo "❌ **部分CI任务失败**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 失败详情" >> $GITHUB_STEP_SUMMARY
            for job in "${FAILED_JOBS[@]}"; do
              echo "- ❌ $job" >> $GITHUB_STEP_SUMMARY
            done
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 排查建议" >> $GITHUB_STEP_SUMMARY
            echo "1. 查看失败的Job日志" >> $GITHUB_STEP_SUMMARY
            echo "2. 检查代码格式和类型错误" >> $GITHUB_STEP_SUMMARY
            echo "3. 确认依赖安装是否成功" >> $GITHUB_STEP_SUMMARY
            echo "4. 检查构建产物是否存在" >> $GITHUB_STEP_SUMMARY
            
            echo "::error::CI任务失败: ${FAILED_JOBS[*]}"
            echo "后端状态: $BACKEND_STATUS"
            echo "前端状态: $FRONTEND_STATUS"
            exit 1
          fi
          
          echo "✅ **所有CI任务通过**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "所有检查已完成，代码质量良好。" >> $GITHUB_STEP_SUMMARY
          echo "✅ 所有CI任务通过"
