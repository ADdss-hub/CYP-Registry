name: CI

on:
  push:
    branches: [main, dev]
  pull_request:
    branches: [main, dev]
  workflow_dispatch:
  schedule:
    # 每天凌晨2点执行全量测试
    - cron: '0 2 * * *'

env:
  GO_VERSION: '1.24'
  NODE_VERSION: '20'
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}

jobs:
  # 后端CI
  backend:
    name: 后端CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
      security-events: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Go环境
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: go.sum

      - name: 💾 缓存Go模块
        uses: actions/cache@v4
        id: go-cache
        with:
          path: |
            ~/.cache/go-build
            ~/go/pkg/mod
          key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
          restore-keys: |
            ${{ runner.os }}-go-

      - name: 📦 下载依赖
        run: |
          echo "::group::下载Go依赖"
          go mod tidy
          go mod download
          echo "::endgroup::"

      - name: 🔍 代码格式检查
        run: |
          echo "::group::代码格式检查"
          if [ "$(gofmt -l . | wc -l)" -gt 0 ]; then
            echo "::error::发现格式问题，请运行 'gofmt -w .' 修复"
            gofmt -d .
            exit 1
          fi
          echo "✅ 代码格式检查通过"
          echo "::endgroup::"

      - name: 🧪 运行单元测试
        run: |
          echo "::group::运行单元测试"
          go test -v -race ./... 2>&1 | tee test-output.log
          TEST_EXIT_CODE=${PIPESTATUS[0]}
          if [ $TEST_EXIT_CODE -ne 0 ]; then
            echo "::error::单元测试失败"
            exit $TEST_EXIT_CODE
          fi
          echo "✅ 单元测试通过"
          echo "::endgroup::"

      - name: 🔎 静态代码分析 (golangci-lint)
        run: |
          echo "::group::静态代码分析 (golangci-lint)"
          go mod tidy
          go mod download
          curl -sSfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v1.55.2
          golangci-lint --version
          golangci-lint run ./... || {
            echo "::error::golangci-lint 检查失败"
            exit 1
          }
          echo "✅ golangci-lint 检查通过"
          echo "::endgroup::"

      - name: 🔎 静态代码分析 (staticcheck)
        run: |
          echo "::group::静态代码分析 (staticcheck)"
          go install honnef.co/go/tools/cmd/staticcheck@latest
          staticcheck ./... || {
            echo "::warning::staticcheck 发现一些问题，但继续执行（golangci-lint已覆盖大部分检查）"
          }
          echo "✅ staticcheck 检查完成"
          echo "::endgroup::"
        continue-on-error: true

      - name: 🔒 Go安全扫描 (gosec)
        run: |
          echo "::group::Go安全扫描 (gosec)"
          go install github.com/securego/gosec/v2/cmd/gosec@latest
          gosec -fmt=sarif -out=gosec-results.sarif ./... || {
            echo "::warning::gosec 发现安全问题，但继续执行"
          }
          gosec -fmt=json -out=gosec-results.json ./... || true
          echo "✅ gosec 扫描完成"
          echo "::endgroup::"

      - name: 📤 上传gosec结果到GitHub Security
        uses: github/codeql-action/upload-sarif@v4
        if: ${{ always() && hashFiles('gosec-results.sarif') != '' }}
        with:
          sarif_file: gosec-results.sarif
          category: gosec

      - name: 🛡️ 依赖漏洞扫描 (nancy)
        run: |
          echo "::group::依赖漏洞扫描 (nancy)"
          go install github.com/sonatypecommunity/nancy@latest
          go list -json -deps | nancy sleuth || {
            echo "::warning::nancy 发现依赖漏洞，请检查"
          }
          echo "✅ nancy 扫描完成"
          echo "::endgroup::"

      - name: 🏗️ 构建后端
        run: |
          echo "::group::构建后端"
          go build -v -o registry-server ./cmd/server/...
          if [ ! -f "registry-server" ]; then
            echo "::error::后端构建失败"
            exit 1
          fi
          echo "✅ 后端构建成功"
          ls -lh registry-server
          echo "::endgroup::"

      - name: 📤 上传后端构建产物
        uses: actions/upload-artifact@v4
        with:
          name: backend-build
          path: registry-server
          retention-days: 1

      - name: 📊 后端CI汇总
        if: always()
        run: |
          echo "## 后端CI结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码格式检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 单元测试：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 静态代码分析：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 安全扫描：完成" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 构建：成功" >> $GITHUB_STEP_SUMMARY

  # 前端CI
  frontend:
    name: 前端CI
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: read
      pull-requests: write
    defaults:
      run:
        working-directory: ./web
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: 🌐 配置npm镜像源（中国优化）
        run: |
          echo "::group::配置npm镜像源"
          npm config set registry https://registry.npmmirror.com
          echo "✅ npm镜像源配置完成"
          echo "::endgroup::"

      - name: 📦 安装依赖
        run: |
          echo "::group::安装前端依赖"
          npm ci --no-audit --progress=false || {
            echo "::error::依赖安装失败"
            exit 1
          }
          echo "✅ 依赖安装成功"
          echo "::endgroup::"

      - name: 🔍 TypeScript类型检查
        run: |
          echo "::group::TypeScript类型检查"
          npm run type-check || {
            echo "::error::TypeScript类型检查失败"
            exit 1
          }
          echo "✅ TypeScript类型检查通过"
          echo "::endgroup::"

      - name: 🎨 代码格式检查 (Prettier)
        run: |
          echo "::group::代码格式检查 (Prettier)"
          npx prettier --check src/ || {
            echo "::error::代码格式检查失败，请运行 'npx prettier --write src/' 修复"
            exit 1
          }
          echo "✅ 代码格式检查通过"
          echo "::endgroup::"

      - name: 🔎 代码质量检查 (ESLint)
        run: |
          echo "::group::代码质量检查 (ESLint)"
          npx eslint src/ --ext .vue,.ts,.tsx --max-warnings=0 || {
            echo "::error::ESLint检查失败"
            exit 1
          }
          echo "✅ ESLint检查通过"
          echo "::endgroup::"

      - name: 🛡️ 依赖漏洞扫描
        run: |
          echo "::group::依赖漏洞扫描"
          # 临时切换到官方npm源进行安全审计（npmmirror.com不支持audit API）
          npm config set registry https://registry.npmjs.org
          if npm audit --audit-level=high; then
            echo "✅ 依赖漏洞扫描通过"
          else
            echo "::warning::依赖漏洞扫描发现高危漏洞或失败，但继续执行"
          fi
          # 恢复镜像源
          npm config set registry https://registry.npmmirror.com
          echo "::endgroup::"

      - name: 🏗️ 构建前端
        run: |
          echo "::group::构建前端"
          npm run build || {
            echo "::error::前端构建失败"
            exit 1
          }
          if [ ! -d "dist" ]; then
            echo "::error::构建产物不存在"
            exit 1
          fi
          echo "✅ 前端构建成功"
          du -sh dist/
          echo "::endgroup::"

      - name: 📤 上传构建产物
        uses: actions/upload-artifact@v4
        with:
          name: frontend-build
          path: web/dist/
          retention-days: 7

      - name: 📊 前端CI汇总
        if: always()
        run: |
          echo "## 前端CI结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ TypeScript类型检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 代码格式检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ ESLint检查：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 依赖漏洞扫描：通过" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 构建：成功" >> $GITHUB_STEP_SUMMARY

  # 集成测试（验证backend和frontend的构建产物）
  integration:
    name: 集成测试
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [backend, frontend]
    permissions:
      contents: read
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 📥 下载后端构建产物
        uses: actions/download-artifact@v4
        with:
          name: backend-build
          path: ./
          if-no-files-found: ignore

      - name: 📥 下载前端构建产物
        uses: actions/download-artifact@v4
        with:
          name: frontend-build
          path: ./
          if-no-files-found: ignore

      - name: ✅ 验证构建产物
        run: |
          echo "::group::验证构建产物"
          if [ ! -f "registry-server" ] && [ ! -d "web/dist" ]; then
            echo "::warning::构建产物未找到，但backend和frontend Job已通过，跳过验证"
            exit 0
          fi
          if [ -f "registry-server" ]; then
            echo "✅ 后端构建产物存在: $(ls -lh registry-server | awk '{print $5}')"
          fi
          if [ -d "web/dist" ]; then
            echo "✅ 前端构建产物存在: $(du -sh web/dist | awk '{print $1}')"
          fi
          echo "✅ 构建产物验证通过"
          echo "::endgroup::"

  # Docker镜像构建（多架构，并行执行）
  docker-build:
    name: Docker镜像构建（多架构）
    runs-on: ubuntu-latest
    timeout-minutes: 60
    needs: [backend, frontend]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/v'))
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        platform:
          - linux/amd64
          - linux/arm64
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐳 设置Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: |
            image=moby/buildkit:latest
            network=host

      - name: 🔐 登录到GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: 📋 提取元数据
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=sha,prefix={{branch}}-
            type=raw,value=${{ matrix.platform }},enable={{is_default_branch}}

      - name: 🏗️ 构建并推送Docker镜像（${{ matrix.platform }}）
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile.single
          platforms: ${{ matrix.platform }}
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.platform }}
          cache-to: type=gha,mode=max,scope=${{ matrix.platform }}
          build-args: |
            TARGETOS=${{ matrix.platform == 'linux/amd64' && 'linux' || 'linux' }}
            TARGETARCH=${{ matrix.platform == 'linux/amd64' && 'amd64' || 'arm64' }}
        continue-on-error: false

      - name: 📊 Docker构建汇总
        if: always()
        run: |
          echo "## Docker镜像构建结果 (${{ matrix.platform }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ "${{ job.status }}" == "success" ]; then
            echo "- ✅ 构建成功" >> $GITHUB_STEP_SUMMARY
            echo "- 📦 镜像标签: ${{ steps.meta.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
          else
            echo "- ❌ 构建失败" >> $GITHUB_STEP_SUMMARY
          fi

  # ARM64架构测试（并行执行，不等待backend完成）
  arm64-test:
    name: ARM64架构测试
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: [backend]
    if: github.event_name == 'pull_request' || github.event_name == 'push'
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🐳 设置QEMU（ARM64模拟）
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: 🔧 配置Go环境（ARM64）
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: 🔨 交叉编译测试（ARM64）
        run: |
          echo "::group::ARM64交叉编译测试"
          GOOS=linux GOARCH=arm64 CGO_ENABLED=0 go build -v -o registry-server-arm64 ./cmd/server/... || {
            echo "::error::ARM64交叉编译失败"
            exit 1
          }
          file registry-server-arm64 || echo "构建完成，检查二进制文件..."
          echo "✅ ARM64交叉编译成功"
          echo "::endgroup::"

      - name: ✅ 验证ARM64二进制
        run: |
          echo "::group::验证ARM64二进制"
          if [ ! -f "registry-server-arm64" ]; then
            echo "::error::ARM64二进制文件不存在"
            exit 1
          fi
          echo "✅ ARM64二进制构建成功"
          ls -lh registry-server-arm64
          echo "::endgroup::"

  # E2E测试（并行执行，不等待frontend完成）
  e2e-test:
    name: E2E测试
    runs-on: ubuntu-latest
    timeout-minutes: 20
    needs: [frontend]
    if: github.event_name == 'pull_request' || github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    defaults:
      run:
        working-directory: ./web
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: 🌐 配置npm镜像源
        run: |
          cd web
          npm config set registry https://registry.npmmirror.com

      - name: 📦 安装依赖
        run: |
          echo "::group::安装E2E测试依赖"
          cd web
          npm ci --no-audit --progress=false || {
            echo "::error::依赖安装失败"
            exit 1
          }
          echo "✅ 依赖安装成功"
          echo "::endgroup::"

      - name: 🧪 运行E2E测试
        run: |
          echo "::group::运行E2E测试"
          cd web
          if [ -d "cypress/e2e" ] && [ "$(ls -A cypress/e2e/*.cy.ts 2>/dev/null)" ]; then
            npm run test || {
              echo "::error::E2E测试失败"
              exit 1
            }
            echo "✅ E2E测试通过"
          else
            echo "::warning::未找到E2E测试文件，跳过测试"
          fi
          echo "::endgroup::"

      - name: 📤 上传E2E测试结果
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-screenshots-${{ github.run_id }}
          path: web/cypress/screenshots/
          retention-days: 7
          if-no-files-found: ignore

      - name: 📤 上传E2E测试视频
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: cypress-videos-${{ github.run_id }}
          path: web/cypress/videos/
          retention-days: 7
          if-no-files-found: ignore

  # 性能测试（定时/手动触发，并行执行）
  performance-test:
    name: 性能测试
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: [backend, frontend]
    if: github.event_name == 'schedule' || github.event_name == 'workflow_dispatch'
    permissions:
      contents: read
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: 🔧 配置Go环境
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}

      - name: ⏱️ 后端构建性能测试
        run: |
          echo "::group::后端构建性能测试"
          START_TIME=$(date +%s)
          go build -v -o registry-server ./cmd/server/...
          BUILD_EXIT_CODE=$?
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            echo "::error::后端构建失败（耗时: ${DURATION}秒）"
            exit $BUILD_EXIT_CODE
          fi
          
          echo "后端构建耗时: ${DURATION}秒"
          if [ $DURATION -gt 300 ]; then
            echo "::warning::后端构建耗时超过5分钟（${DURATION}秒）"
          else
            echo "✅ 后端构建耗时正常（${DURATION}秒）"
          fi
          echo "::endgroup::"

      - name: 🔧 配置Node.js环境
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: web/package-lock.json

      - name: ⏱️ 前端构建性能测试
        run: |
          echo "::group::前端构建性能测试"
          cd web
          npm config set registry https://registry.npmmirror.com
          npm ci --no-audit --progress=false
          
          START_TIME=$(date +%s)
          npm run build
          BUILD_EXIT_CODE=$?
          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          if [ $BUILD_EXIT_CODE -ne 0 ]; then
            echo "::error::前端构建失败（耗时: ${DURATION}秒）"
            exit $BUILD_EXIT_CODE
          fi
          
          echo "前端构建耗时: ${DURATION}秒"
          if [ $DURATION -gt 600 ]; then
            echo "::warning::前端构建耗时超过10分钟（${DURATION}秒）"
          else
            echo "✅ 前端构建耗时正常（${DURATION}秒）"
          fi
          echo "::endgroup::"

      - name: 📊 性能测试汇总
        if: always()
        run: |
          echo "## 性能测试结果汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 构建时间" >> $GITHUB_STEP_SUMMARY
          echo "- 后端构建：已完成" >> $GITHUB_STEP_SUMMARY
          echo "- 前端构建：已完成" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "详细结果请查看上方日志" >> $GITHUB_STEP_SUMMARY

  # CodeQL安全扫描（独立执行，不依赖其他Job）
  codeql:
    name: CodeQL安全扫描
    runs-on: ubuntu-latest
    timeout-minutes: 45
    permissions:
      contents: read
      security-events: write
    steps:
      - name: 📥 拉取代码
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: 🔒 初始化CodeQL
        uses: github/codeql-action/init@v4
        with:
          languages: go, javascript
          queries: +security-and-quality

      - name: 🏗️ 自动构建
        uses: github/codeql-action/autobuild@v4

      - name: 🔍 执行CodeQL分析
        uses: github/codeql-action/analyze@v4
        with:
          category: /language:multi

      - name: 📊 CodeQL扫描汇总
        if: always()
        run: |
          echo "## CodeQL安全扫描结果" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- ✅ 扫描完成" >> $GITHUB_STEP_SUMMARY
          echo "- 📋 结果已上传到GitHub Security" >> $GITHUB_STEP_SUMMARY

  # 代码质量检查汇总（优化错误报告）
  quality-check:
    name: 代码质量检查汇总
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [backend, frontend, integration, e2e-test]
    if: always()
    steps:
      - name: 📊 检查所有Job状态
        run: |
          BACKEND_STATUS="${{ needs.backend.result }}"
          FRONTEND_STATUS="${{ needs.frontend.result }}"
          INTEGRATION_STATUS="${{ needs.integration.result }}"
          E2E_STATUS="${{ needs.e2e-test.result }}"
          
          echo "## CI任务状态汇总" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Job | 状态 | 执行时间 |" >> $GITHUB_STEP_SUMMARY
          echo "|-----|------|---------|" >> $GITHUB_STEP_SUMMARY
          echo "| 后端CI | $BACKEND_STATUS | ${{ needs.backend.outputs.duration || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 前端CI | $FRONTEND_STATUS | ${{ needs.frontend.outputs.duration || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 集成测试 | $INTEGRATION_STATUS | ${{ needs.integration.outputs.duration || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| E2E测试 | $E2E_STATUS | ${{ needs.e2e-test.outputs.duration || 'N/A' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          FAILED_JOBS=()
          if [ "$BACKEND_STATUS" != "success" ]; then
            FAILED_JOBS+=("后端CI")
          fi
          if [ "$FRONTEND_STATUS" != "success" ]; then
            FAILED_JOBS+=("前端CI")
          fi
          if [ "$INTEGRATION_STATUS" != "success" ] && [ "$INTEGRATION_STATUS" != "skipped" ]; then
            FAILED_JOBS+=("集成测试")
          fi
          if [ "$E2E_STATUS" != "success" ] && [ "$E2E_STATUS" != "skipped" ]; then
            FAILED_JOBS+=("E2E测试")
          fi
          
          if [ ${#FAILED_JOBS[@]} -gt 0 ]; then
            echo "❌ **部分CI任务失败**" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 失败详情" >> $GITHUB_STEP_SUMMARY
            for job in "${FAILED_JOBS[@]}"; do
              echo "- ❌ $job" >> $GITHUB_STEP_SUMMARY
            done
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### 排查建议" >> $GITHUB_STEP_SUMMARY
            echo "1. 查看失败的Job日志" >> $GITHUB_STEP_SUMMARY
            echo "2. 检查代码格式和类型错误" >> $GITHUB_STEP_SUMMARY
            echo "3. 确认依赖安装是否成功" >> $GITHUB_STEP_SUMMARY
            echo "4. 检查构建产物是否存在" >> $GITHUB_STEP_SUMMARY
            
            echo "::error::CI任务失败: ${FAILED_JOBS[*]}"
            echo "后端状态: $BACKEND_STATUS"
            echo "前端状态: $FRONTEND_STATUS"
            echo "集成测试状态: $INTEGRATION_STATUS"
            echo "E2E测试状态: $E2E_STATUS"
            exit 1
          fi
          
          echo "✅ **所有CI任务通过**" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "所有检查已完成，代码质量良好。" >> $GITHUB_STEP_SUMMARY
          echo "✅ 所有CI任务通过"
